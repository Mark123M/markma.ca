import { createTheme } from '@mui/material/styles';

const mytheme = createTheme({
  palette: {
    mytext: {
      main: '#6c584c',
    },
    mytext2: {
      main: '#e9946b'
    },
    mytitle: {
      main: '#412a24'
    }
  },
});

export default mytheme